{
  "name": "forensai",
  "version": "1.0.0",
  "private": true,
  "description": "ForensAI — builds the React frontend and installs backend dependencies so a single Node process (backend/src/server.js) can serve both.",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start"
  }
}
